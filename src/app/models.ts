export interface IPartner {
  image: string;
  name: string;
  link: string;
  addName: boolean;
}

export interface IPerson {
  name: string;
  designation: string;
  organisation: string;
  photoUrl: string;
}